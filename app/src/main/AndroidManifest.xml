<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.company.mawarees">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">


        <activity
            android:name=".View.activity.ResultActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|locale|screenSize|screenLayout|uiMode"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity
            android:name=".View.activity.ProblemActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|locale|screenSize|screenLayout|uiMode"
            android:theme="@style/AppTheme.NoActionBar"
            tools:ignore="InnerclassSeparator">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>